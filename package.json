{
  "name": "fullsupply-rebuild-1",
  "version": "0.1.0",
  "description": "Clean Eleventy + vanilla CSS rebuild of the Full Supply website.",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
